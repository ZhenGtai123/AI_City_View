# AI City View - 依赖包

# 核心库
opencv-python>=4.8.0
numpy>=1.24.0,<2
Pillow>=10.0.0

# PyTorch — 不在这里装！按 README 步骤，最后用 --force-reinstall 装 CUDA 版:
# pip install --force-reinstall torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# 语义分割 (OneFormer ADE20K)
transformers>=4.30.0
accelerate>=0.20.0

# Pipeline 依赖
scikit-learn>=1.3.0
scipy>=1.10.0

# 深度估计 (Depth Anything V3, 需从 GitHub 安装)
# pip install git+https://github.com/ByteDance-Seed/depth-anything-3.git

# FastAPI Server
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# 工具
tqdm>=4.65.0

# 云批量处理 (cloud_batch_run.py, 按需安装)
# pip install azure-storage-blob google-cloud-storage

# 可选: GPU 加速
# pip install xformers
