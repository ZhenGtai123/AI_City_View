# AI City View - 依赖包

# 核心库
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# PyTorch (建议通过 --index-url 单独安装 CUDA 版本，见 README)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# 语义分割 (OneFormer ADE20K)
transformers>=4.30.0
accelerate>=0.20.0

# Pipeline 依赖
scikit-learn>=1.3.0
scipy>=1.10.0

# 深度估计 (Depth Anything V3, 需从 GitHub 安装)
# pip install git+https://github.com/ByteDance-Seed/depth-anything-3.git

# FastAPI Server
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# 可选: GPU 加速
# pip install xformers
